// very simple hof example

function thing = fn(x: function) -> i32 {
  return x(1);
};

println(thing(fn(x: i32) -> i32 { return x + 1; }));



// curring example

function makeadder = fn(numbertoadd: i32) -> function {
  
  function newadder = fn(n: i32) -> i32 {
    return numbertoadd + n; 
  };
  return newadder;
};

// TODO: why does this not work
// function myadder = makeadder(10);

