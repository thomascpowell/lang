// currying example

function adder = fn(num: i32) -> function {
  return fn(n: i32) -> i32 {
    return num + n; 
  };
};

function myadder = adder(100);

// the langauge does not support closures yet
// so this particular example will not be able to access num
println(myadder(1));
