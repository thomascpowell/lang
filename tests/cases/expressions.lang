if (1 < 2) {
  println("hello")
} else {
  panic("this will not run")
};

1 + 1;

fn() -> i32 {
  return 1;
};

1 + 1 + 1 * 1000;

7;

string whatever = "hello";

// this is valid, but is doesn't do anything
whatever();

// yet another thing that you can do
7(3);

"a string";

// this would throw an error
// true;
// false;



// simple demonstration of recursion
def recursive = fn(param: i32) -> i32 {
  println("param = ", param);
  return if (param > 100) {
    recursive(param/2)
  } else {
    param
  };
};

recursive(1000);
