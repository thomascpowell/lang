if (1 < 2) {
  1 + 2
} else {
  panic("this will not run")
};

1 + 1;

fn() -> i32 {
  return 1;
};

1 + 1 + 1 * 1000;

7;

string whatever = "hello";

whatever();

7(3);

"a string";

// simple demonstration of recursion
function recursive = fn(param: i32) -> i32 {
  return if (param > 100) {
    recursive(param/2)
  } else {
    param
  };
};

recursive(1000);
